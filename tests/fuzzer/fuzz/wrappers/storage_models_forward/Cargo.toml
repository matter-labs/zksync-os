[package]
name = "storage_models_forward"
resolver = "2"
version = "0.1.0"
edition = "2021"
authors = ["The Matter Labs Team <hello@matterlabs.dev>"]
homepage = "https://zksync.io/"
repository = "https://github.com/matter-labs/zksync-os/"
license = "MIT OR Apache-2.0"
keywords = ["blockchain", "zksync", "zk", "risc-v"]
categories = ["cryptography"]

[lib]
path = "../../../../../storage_models/src/lib.rs"

[dependencies]
zk_ee = { package = "zk_ee_forward", path = "../zk_ee_forward" }
crypto = { package = "crypto_forward", path = "../crypto_forward", default-features = false }

[features]
testing = ["zk_ee/testing"]
evm_refunds = ["zk_ee/evm_refunds"]
