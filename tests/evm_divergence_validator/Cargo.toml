[package]
name = "evm_divergence_validator"
version = "0.1.0"
edition = "2021"
authors = ["The Matter Labs Team <hello@matterlabs.dev>"]
homepage = "https://zksync.io/"
repository = "https://github.com/matter-labs/zksync-os/"
license = "MIT OR Apache-2.0"
keywords = ["blockchain", "zksync", "zk", "risc-v"]
categories = ["cryptography"]

[[bin]]
name = "evm-divergence-validator"
path = "src/main.rs"

[dependencies]
anyhow = "1"
serde = { workspace = true, features = ["std", "derive"] }
serde_json = { workspace = true }
serde_yaml = "0.9"
alloy = { workspace = true, features = [
    "consensus",
    "dyn-abi",
    "json-abi",
    "network",
    "signers",
    "signer-local",
    "sol-types",
] }
ruint = { workspace = true, features = ["alloc"] }
log = { workspace = true }
hex = { workspace = true }
tempfile = "3"

rig = { path = "../rig", features = ["no_print", "unlimited_native"] }
zksync_os_tests_common = { path = "../common" }
zksync_os_revm_runner = { path = "../revm_runner" }
zk_ee = { path = "../../zk_ee" }
forward_system = { path = "../../forward_system" }
