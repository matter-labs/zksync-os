diff --git a/callable_oracles/src/arithmetic/mod.rs b/callable_oracles/src/arithmetic/mod.rs
index b79e70ca..d1f12026 100644
--- a/callable_oracles/src/arithmetic/mod.rs
+++ b/callable_oracles/src/arithmetic/mod.rs
@@ -3,7 +3,7 @@ use basic_system::system_functions::modexp::{
 };
 use oracle_provider::OracleQueryProcessor;
 use oracle_provider::RamPeek;
-use zk_ee::oracle::query_ids::U256_MULMOD_ADVICE_QUERY_ID;
+use zk_ee::oracle::query_ids::{U256_DIV_REM_ADVICE_QUERY_ID, U256_MULMOD_ADVICE_QUERY_ID};
 
 use crate::utils::{
     evaluate::{read_memory_as_u64, read_struct},
@@ -11,6 +11,7 @@ use crate::utils::{
 };
 use crate::{read_host_struct, read_u64_words};
 
+const _: () = assert!(U256_DIV_REM_ADVICE_QUERY_ID == 0x4005_0030);
 const _: () = assert!(U256_MULMOD_ADVICE_QUERY_ID == 0x4005_0031);
 
 struct ArithmeticQueryOutput {
@@ -74,6 +75,26 @@ fn push_limbs(result: &mut Vec<usize>, limbs: &[u64]) {
     }
 }
 
+/// Handle U256 div_rem oracle query.
+///
+/// Input: 8 usizes = dividend (4 limbs) + divisor (4 limbs).
+/// Output: 8 usizes = quotient (4 limbs) + remainder (4 limbs).
+fn process_u256_div_rem_query(
+    query: Vec<usize>,
+) -> Box<dyn ExactSizeIterator<Item = usize> + 'static + Send + Sync> {
+    let mut it = query.into_iter();
+    let mut dividend = read_u256_limbs(&mut it);
+    let mut divisor = read_u256_limbs(&mut it);
+    assert!(it.next().is_none(), "expected exactly 8 usizes for div_rem");
+
+    ruint::algorithms::div(&mut dividend, &mut divisor);
+
+    let mut result = Vec::with_capacity(8);
+    push_limbs(&mut result, &dividend);
+    push_limbs(&mut result, &divisor);
+    Box::new(UsizeSliceIteratorOwned::new(result.into_boxed_slice()))
+}
+
 /// Handle U256 mulmod oracle query.
 ///
 /// Input: 12 usizes = a (4 limbs) + b (4 limbs) + m (4 limbs).
@@ -175,7 +196,11 @@ pub struct ArithmeticQuery;
 
 impl OracleQueryProcessor for ArithmeticQuery {
     fn supported_query_ids(&self) -> Vec<u32> {
-        vec![MODEXP_ADVICE_QUERY_ID, U256_MULMOD_ADVICE_QUERY_ID]
+        vec![
+            MODEXP_ADVICE_QUERY_ID,
+            U256_DIV_REM_ADVICE_QUERY_ID,
+            U256_MULMOD_ADVICE_QUERY_ID,
+        ]
     }
 
     fn process_buffered_query(
@@ -186,6 +211,10 @@ impl OracleQueryProcessor for ArithmeticQuery {
     ) -> Box<dyn ExactSizeIterator<Item = usize> + 'static + Send + Sync> {
         debug_assert!(self.supports_query_id(query_id));
 
+        if query_id == U256_DIV_REM_ADVICE_QUERY_ID {
+            return process_u256_div_rem_query(query);
+        }
+
         if query_id == U256_MULMOD_ADVICE_QUERY_ID {
             return process_u256_mulmod_query(query);
         }
@@ -196,15 +225,19 @@ impl OracleQueryProcessor for ArithmeticQuery {
 
 /// Query processor to be used for prover input native run.
 /// Works in a similar way as the ArithmeticQuery, but with
-/// 64-bit pointers for modexp. For U256 mulmod, the input
-/// format is value-based (limbs in the query Vec), so the
-/// same handler works for both RISC-V and native.
+/// 64-bit pointers for modexp. For U256 div_rem and mulmod,
+/// the input format is value-based (limbs in the query Vec),
+/// so the same handlers work for both RISC-V and native.
 #[derive(Default)]
 pub struct NativeArithmeticQuery;
 
 impl OracleQueryProcessor for NativeArithmeticQuery {
     fn supported_query_ids(&self) -> Vec<u32> {
-        vec![MODEXP_ADVICE_QUERY_ID, U256_MULMOD_ADVICE_QUERY_ID]
+        vec![
+            MODEXP_ADVICE_QUERY_ID,
+            U256_DIV_REM_ADVICE_QUERY_ID,
+            U256_MULMOD_ADVICE_QUERY_ID,
+        ]
     }
 
     fn process_buffered_query(
@@ -215,6 +248,10 @@ impl OracleQueryProcessor for NativeArithmeticQuery {
     ) -> Box<dyn ExactSizeIterator<Item = usize> + 'static + Send + Sync> {
         debug_assert!(self.supports_query_id(query_id));
 
+        if query_id == U256_DIV_REM_ADVICE_QUERY_ID {
+            return process_u256_div_rem_query(query);
+        }
+
         if query_id == U256_MULMOD_ADVICE_QUERY_ID {
             return process_u256_mulmod_query(query);
         }
@@ -394,6 +431,32 @@ mod tests {
         assert_eq!(output[2], 1);
     }
 
+    #[test]
+    fn u256_div_rem_query_basic() {
+        // 10 / 3 = q=3, r=1. Input as value-based limbs.
+        let query: Vec<usize> = vec![10, 0, 0, 0, 3, 0, 0, 0];
+        let output: Vec<usize> = process_u256_div_rem_query(query).collect();
+        assert_eq!(output, vec![3, 0, 0, 0, 1, 0, 0, 0]);
+    }
+
+    #[test]
+    fn u256_div_rem_via_arithmetic_query() {
+        let query: Vec<usize> = vec![10, 0, 0, 0, 3, 0, 0, 0];
+        let output: Vec<usize> = ArithmeticQuery
+            .process_buffered_query(U256_DIV_REM_ADVICE_QUERY_ID, query, &DummyMemorySource)
+            .collect();
+        assert_eq!(output, vec![3, 0, 0, 0, 1, 0, 0, 0]);
+    }
+
+    #[test]
+    fn u256_div_rem_via_native_query() {
+        let query: Vec<usize> = vec![10, 0, 0, 0, 3, 0, 0, 0];
+        let output: Vec<usize> = NativeArithmeticQuery
+            .process_buffered_query(U256_DIV_REM_ADVICE_QUERY_ID, query, &DummyMemorySource)
+            .collect();
+        assert_eq!(output, vec![3, 0, 0, 0, 1, 0, 0, 0]);
+    }
+
     #[test]
     fn u256_mulmod_query_basic() {
         // a=7, b=5, m=6 → a*b=35, q=5, r=5
diff --git a/evm_interpreter/src/instructions/arithmetic.rs b/evm_interpreter/src/instructions/arithmetic.rs
index d230521a..d87ee9b1 100644
--- a/evm_interpreter/src/instructions/arithmetic.rs
+++ b/evm_interpreter/src/instructions/arithmetic.rs
@@ -29,12 +29,19 @@ impl<S: EthereumLikeTypes> Interpreter<'_, S> {
         Ok(())
     }
 
-    pub fn div(&mut self) -> InstructionResult {
+    pub fn div(&mut self, system: &mut System<S>) -> InstructionResult
+    where
+        S::IO: IOSubsystemExt,
+    {
         self.gas
             .spend_gas_and_native(gas_constants::LOW, DIV_NATIVE_COST)?;
         let (op1, op2) = self.stack.pop_1_mut_and_peek()?;
         if !op2.is_zero() {
-            U256::div_rem(op1, op2);
+            zk_ee::utils::u256_arithmetic_advice::u256_div_rem_with_advice(
+                op1,
+                op2,
+                system.io.oracle(),
+            );
             Clone::clone_from(op2, &*op1);
         }
         Ok(())
diff --git a/evm_interpreter/src/interpreter.rs b/evm_interpreter/src/interpreter.rs
index 73d8552b..aac2828a 100644
--- a/evm_interpreter/src/interpreter.rs
+++ b/evm_interpreter/src/interpreter.rs
@@ -157,7 +157,7 @@ impl<'ee, S: EthereumLikeTypes> Interpreter<'ee, S> {
                     opcodes::ADD => self.wrapped_add(),
                     opcodes::MUL => self.wrapping_mul(),
                     opcodes::SUB => self.wrapping_sub(),
-                    opcodes::DIV => self.div(),
+                    opcodes::DIV => self.div(system),
                     opcodes::SDIV => self.sdiv(),
                     opcodes::MOD => self.rem(),
                     opcodes::SMOD => self.smod(),
diff --git a/zk_ee/src/oracle/query_ids.rs b/zk_ee/src/oracle/query_ids.rs
index c4cccc22..eb6c1826 100644
--- a/zk_ee/src/oracle/query_ids.rs
+++ b/zk_ee/src/oracle/query_ids.rs
@@ -42,6 +42,11 @@ pub const ADVICE_SUBSPACE_MASK: u32 = BASIC_SUBSPACE_MASK | 0x00_05_00_00; // 0x
 
 // ========== Advice Subspace Queries ==========
 
+/// Query to get division hint (quotient, remainder) for U256 values.
+/// Guest sends pointers to dividend and divisor; host returns q and r.
+/// Guest verifies q * d + r == n and r < d using delegated arithmetic.
+pub const U256_DIV_REM_ADVICE_QUERY_ID: u32 = ADVICE_SUBSPACE_MASK | 0x30; // 0x40050030
+
 /// Query to get mulmod hint (quotient, remainder) for U256 values.
 /// Guest sends pointers to a, b, and modulus; host returns q and r
 /// such that a*b == q*m + r and r < m.
diff --git a/zk_ee/src/utils/u256_arithmetic_advice.rs b/zk_ee/src/utils/u256_arithmetic_advice.rs
index 195e7af6..0384eaf7 100644
--- a/zk_ee/src/utils/u256_arithmetic_advice.rs
+++ b/zk_ee/src/utils/u256_arithmetic_advice.rs
@@ -1,4 +1,4 @@
-use crate::oracle::query_ids::U256_MULMOD_ADVICE_QUERY_ID;
+use crate::oracle::query_ids::{U256_DIV_REM_ADVICE_QUERY_ID, U256_MULMOD_ADVICE_QUERY_ID};
 use crate::oracle::usize_serialization::UsizeDeserializable;
 use crate::oracle::IOOracle;
 use u256::U256;
@@ -13,6 +13,49 @@ fn read_limbs_from_oracle_response(it: &mut impl ExactSizeIterator<Item = usize>
     ]
 }
 
+/// Oracle-backed U256 division with remainder.
+///
+/// Delegation cost: 5 calls (2 MUL + 1 ADD + 1 SUB + 1 EQ).
+#[inline]
+pub fn u256_div_rem_with_advice<O: IOOracle>(
+    dividend_or_quotient: &mut U256,
+    divisor_or_remainder: &mut U256,
+    oracle: &mut O,
+) {
+    assert!(!divisor_or_remainder.is_zero());
+
+    let input: [u64; 8] = {
+        let d = dividend_or_quotient.as_limbs();
+        let v = divisor_or_remainder.as_limbs();
+        [d[0], d[1], d[2], d[3], v[0], v[1], v[2], v[3]]
+    };
+
+    let mut it = oracle
+        .raw_query(U256_DIV_REM_ADVICE_QUERY_ID, &input)
+        .expect("div_rem oracle query failed");
+
+    let q_limbs = read_limbs_from_oracle_response(&mut it);
+    let r_limbs = read_limbs_from_oracle_response(&mut it);
+
+    let mut check_lo = U256::from_limbs(q_limbs);
+    let mut check_hi = U256::from_limbs(q_limbs);
+    check_lo.widening_mul_assign_into(&mut check_hi, divisor_or_remainder);
+
+    let remainder = U256::from_limbs(r_limbs);
+    let carry = check_lo.overflowing_add_assign(&remainder);
+
+    core::ops::BitXorAssign::bitxor_assign(&mut check_lo, dividend_or_quotient);
+    core::ops::BitOrAssign::bitor_assign(&mut check_lo, &check_hi);
+    assert!(!carry && check_lo.is_zero());
+
+    let mut r_check = U256::from_limbs(r_limbs);
+    let borrow = r_check.overflowing_sub_assign(divisor_or_remainder);
+    assert!(borrow);
+
+    *dividend_or_quotient = U256::from_limbs(q_limbs);
+    *divisor_or_remainder = remainder;
+}
+
 /// Oracle-backed U256 mulmod: computes `(a * b) % m`.
 ///
 /// Delegation cost: 10 calls (6 MUL + 2 ADD + 1 SUB + 1 EQ),
